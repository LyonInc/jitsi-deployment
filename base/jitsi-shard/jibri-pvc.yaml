kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  namespace: jitsi
  name: jibri-recorder
spec:
  storageClassName: longhorn
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 20Gi
